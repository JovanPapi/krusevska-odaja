export interface Ingredient {
  // uuid?: string; ne e potrebno ako e samo za na FE
  name: string;
  nameTranslated: string;

  // listOfProducts: Product[]; ne e potrebno ako e samo za vo frontend aplikacija
}
